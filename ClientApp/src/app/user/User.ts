/*
export class RegisterData {
  firstName: string;
  lastName: string;
  email: string;
  password: string;
  confPassword: string;

  constructor(
    firstName: string,
    lastName: string,
    email: string,
    password: string,
    confPassword: string
  ) {
    this.firstName = firstName;
    this.lastName = lastName;
    this.email = email;
    this.password = password;
    this.confPassword = password;
  }
}*/

/*export class User {
  constructor(
    public id: number,
    public firstName: string,
    public lastName: string,
    public email: string,
    public profilePicture: string,
    public age: string,
    public passwordHash: string
  ) { }
}

export interface LoginResponse {
  token: string;
  user: User;
}

export class UpdateData {
  id: string;
  profilePicture: string;
  age: string;

  constructor(id: string, profilePicture: string, age: string) {
    this.id = id;
    this.profilePicture = profilePicture;
    this.age = age;
  }
}*/
